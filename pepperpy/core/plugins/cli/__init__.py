"""CLI plugin system integrated with the core plugin system.

This module provides the integration between the CLI and the core plugin system,
allowing CLI commands to be extended through plugins. It includes functionality
for loading, configuring, and executing CLI plugin commands.
"""
