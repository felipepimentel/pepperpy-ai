name: embeddings/huggingface
version: 0.1.0
description: Provider for Huggingface for embeddings tasks
author: PepperPy Team
plugin_type: embeddings
default_config: null
config_schema: null
documentation:
  usage: 'Huggingface provider for embeddings tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("embeddings", "huggingface")


    # Usage example here

    ```

    '
category: embeddings
provider_name: huggingface
entry_point: provider.HuggingFaceEmbeddingProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
