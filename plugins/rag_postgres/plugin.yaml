name: Postgres Rag Provider
version: 0.1.0
description: Provider for Postgres for rag tasks
author: PepperPy Team
plugin_category: rag
provider_type: postgres
default_config: null
config_schema: null
documentation:
  usage: 'Postgres provider for rag tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("rag", "postgres")


    # Usage example here

    ```

    '
category: rag
provider_name: postgres
entry_point: provider.PostgresProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
