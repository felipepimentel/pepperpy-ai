name: workflow/local
version: 0.1.0
description: Provider for Local for workflow tasks
author: PepperPy Team
default_config: null
config_schema: null
documentation:
  usage: 'Local provider for workflow tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("workflow", "local")


    # Usage example here

    ```

    '
category: workflow
provider_name: local
entry_point: provider.LocalExecutor
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
