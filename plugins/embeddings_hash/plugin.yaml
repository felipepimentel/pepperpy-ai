name: Hash Embeddings Provider
version: 0.1.0
description: Provider for Hash for embeddings tasks
author: PepperPy Team
plugin_category: embeddings
provider_type: hash
default_config: null
config_schema: null
documentation:
  usage: 'Hash provider for embeddings tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("embeddings", "hash")


    # Usage example here

    ```

    '
category: embeddings
provider_name: hash
entry_point: provider.HashEmbeddingProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
