# Autogen Agents Provider Plugin Metadata
name: "Autogen Agents Provider"
version: "0.1.0"
description: "Provider for Autogen for agents tasks"
author: "PepperPy Team"
plugin_category: "agents"
provider_type: "autogen"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Autogen provider for agents tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("agents", "autogen")
    
    # Usage example here
    ```
