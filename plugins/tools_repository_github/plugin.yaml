# Github Tools Repository Provider Plugin Metadata
name: "Github Tools Repository Provider"
version: "0.1.0"
description: "Provider for Github for tools_repository tasks"
author: "PepperPy Team"
plugin_category: "tools/repository"
provider_type: "github"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Github provider for tools/repository tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("tools/repository", "github")
    
    # Usage example here
    ```
