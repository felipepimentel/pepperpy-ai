# Local Workflow Provider Plugin Metadata
name: "Local Workflow Provider"
version: "0.1.0"
description: "Provider for Local for workflow tasks"
author: "PepperPy Team"
plugin_category: "workflow"
provider_type: "local"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Local provider for workflow tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("workflow", "local")
    
    # Usage example here
    ```
