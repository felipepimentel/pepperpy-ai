name: storage/rest
version: 0.1.0
description: Provider for Rest for storage tasks
author: PepperPy Team
default_config: null
config_schema: null
documentation:
  usage: 'Rest provider for storage tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("storage", "rest")


    # Usage example here

    ```

    '
category: storage
provider_name: rest
entry_point: provider.RestProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
