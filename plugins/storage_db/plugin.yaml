# Db Storage Provider Plugin Metadata
name: "Db Storage Provider"
version: "0.1.0"
description: "Provider for Db for storage tasks"
author: "PepperPy Team"
plugin_category: "storage"
provider_type: "db"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Db provider for storage tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("storage", "db")
    
    # Usage example here
    ```
