# Pinecone Rag Provider Plugin Metadata
name: "Pinecone Rag Provider"
version: "0.1.0"
description: "Provider for Pinecone for rag tasks"
author: "PepperPy Team"
plugin_category: "rag"
provider_type: "pinecone"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Pinecone provider for rag tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("rag", "pinecone")
    
    # Usage example here
    ```
