name: Sqlite Storage Provider
version: 0.1.0
description: Provider for Sqlite for storage tasks
author: PepperPy Team
plugin_category: storage
provider_type: sqlite
default_config: null
config_schema: null
documentation:
  usage: 'Sqlite provider for storage tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("storage", "sqlite")


    # Usage example here

    ```

    '
category: storage
provider_name: sqlite
entry_point: provider.SqliteProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
