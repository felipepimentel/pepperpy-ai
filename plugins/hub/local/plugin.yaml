name: hub/local
version: 0.1.0
description: Provider for Local for hub tasks
author: PepperPy Team
default_config: null
config_schema: null
documentation:
  usage: 'Local provider for hub tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("hub", "local")


    # Usage example here

    ```

    '
category: hub
provider_name: local
entry_point: provider.LocalHubProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
