# Local Embeddings Provider Plugin Metadata
name: "Local Embeddings Provider"
version: "0.1.0"
description: "Provider for Local for embeddings tasks"
author: "PepperPy Team"
plugin_category: "embeddings"
provider_type: "local"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Local provider for embeddings tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("embeddings", "local")
    
    # Usage example here
    ```
