name: Milvus Rag Provider
version: 0.1.0
description: Provider for Milvus for rag tasks
author: PepperPy Team
plugin_category: rag
provider_type: milvus
default_config: null
config_schema: null
documentation:
  usage: 'Milvus provider for rag tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("rag", "milvus")


    # Usage example here

    ```

    '
category: rag
provider_name: milvus
entry_point: provider.MilvusProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
