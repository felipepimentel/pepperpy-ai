name: embeddings/cohere
version: 0.1.0
description: Provider for Cohere for embeddings tasks
author: PepperPy Team
plugin_type: embeddings
default_config: null
config_schema: null
documentation:
  usage: 'Cohere provider for embeddings tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("embeddings", "cohere")


    # Usage example here

    ```

    '
category: embeddings
provider_name: cohere
entry_point: provider.CohereEmbeddingProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
