name: embeddings/fastai
version: 0.1.0
description: Provider for Fastai for embeddings tasks
author: PepperPy Team
plugin_type: embeddings
default_config: null
config_schema: null
documentation:
  usage: 'Fastai provider for embeddings tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("embeddings", "fastai")


    # Usage example here

    ```

    '
category: embeddings
provider_name: fastai
entry_point: provider.FastAIEmbeddingProvider
required_config_keys: []
