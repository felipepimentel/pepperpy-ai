# Rest Storage Provider Plugin Metadata
name: "Rest Storage Provider"
version: "0.1.0"
description: "Provider for Rest for storage tasks"
author: "PepperPy Team"
plugin_category: "storage"
provider_type: "rest"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Rest provider for storage tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("storage", "rest")
    
    # Usage example here
    ```
