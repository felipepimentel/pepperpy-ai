# Supabase Storage Provider Plugin Metadata
name: "Supabase Storage Provider"
version: "0.1.0"
description: "Provider for Supabase for storage tasks"
author: "PepperPy Team"
plugin_category: "storage"
provider_type: "supabase"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Supabase provider for storage tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("storage", "supabase")
    
    # Usage example here
    ```
