# Ffmpeg Content Processing Audio Provider Plugin Metadata
name: "Ffmpeg Content Processing Audio Provider"
version: "0.1.0"
description: "Provider for Ffmpeg for content_processing_audio tasks"
author: "PepperPy Team"
plugin_category: "content/processing/audio"
provider_type: "ffmpeg"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Ffmpeg provider for content/processing/audio tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("content/processing/audio", "ffmpeg")
    
    # Usage example here
    ```
