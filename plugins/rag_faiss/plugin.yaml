# Faiss Rag Provider Plugin Metadata
name: "Faiss Rag Provider"
version: "0.1.0"
description: "Provider for Faiss for rag tasks"
author: "PepperPy Team"
plugin_category: "rag"
provider_type: "faiss"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Faiss provider for rag tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("rag", "faiss")
    
    # Usage example here
    ```
