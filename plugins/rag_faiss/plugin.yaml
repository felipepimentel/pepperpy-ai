name: Faiss Rag Provider
version: 0.1.0
description: Provider for Faiss for rag tasks
author: PepperPy Team
plugin_category: rag
provider_type: faiss
default_config: null
config_schema: null
documentation:
  usage: 'Faiss provider for rag tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("rag", "faiss")


    # Usage example here

    ```

    '
category: rag
provider_name: faiss
entry_point: provider.FaissProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
