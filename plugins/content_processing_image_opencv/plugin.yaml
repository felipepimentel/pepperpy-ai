# Opencv Content Processing Image Provider Plugin Metadata
name: "Opencv Content Processing Image Provider"
version: "0.1.0"
description: "Provider for Opencv for content_processing_image tasks"
author: "PepperPy Team"
plugin_category: "content/processing/image"
provider_type: "opencv"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Opencv provider for content/processing/image tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("content/processing/image", "opencv")
    
    # Usage example here
    ```
