# Pandoc Content Processing Document Provider Plugin Metadata
name: "Pandoc Content Processing Document Provider"
version: "0.1.0"
description: "Provider for Pandoc for content_processing_document tasks"
author: "PepperPy Team"
plugin_category: "content/processing/document"
provider_type: "pandoc"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Pandoc provider for content/processing/document tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("content/processing/document", "pandoc")
    
    # Usage example here
    ```
