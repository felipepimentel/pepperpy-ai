# Memory Cache Provider Plugin Metadata
name: "Memory Cache Provider"
version: "0.1.0"
description: "Provider for Memory for cache tasks"
author: "PepperPy Team"
plugin_category: "cache"
provider_type: "memory"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Memory provider for cache tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("cache", "memory")
    
    # Usage example here
    ```
