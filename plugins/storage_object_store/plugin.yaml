# Object Store Storage Provider Plugin Metadata
name: "Object Store Storage Provider"
version: "0.1.0"
description: "Provider for Object Store for storage tasks"
author: "PepperPy Team"
plugin_category: "storage"
provider_type: "object_store"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Object Store provider for storage tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("storage", "object_store")
    
    # Usage example here
    ```
