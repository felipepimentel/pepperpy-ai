# Huggingface Embeddings Provider Plugin Metadata
name: "Huggingface Embeddings Provider"
version: "0.1.0"
description: "Provider for Huggingface for embeddings tasks"
author: "PepperPy Team"
plugin_category: "embeddings"
provider_type: "huggingface"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Huggingface provider for embeddings tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("embeddings", "huggingface")
    
    # Usage example here
    ```
