name: Openrouter Embeddings Provider
version: 0.1.0
description: Provider for Openrouter for embeddings tasks
author: PepperPy Team
plugin_category: embeddings
provider_type: openrouter
default_config: null
config_schema: null
documentation:
  usage: 'Openrouter provider for embeddings tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("embeddings", "openrouter")


    # Usage example here

    ```

    '
category: embeddings
provider_name: openrouter
entry_point: provider.OpenrouterProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
