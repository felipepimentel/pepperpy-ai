# Assistant Agents Provider Plugin Metadata
name: "Assistant Agents Provider"
version: "0.1.0"
description: "Provider for Assistant for agents tasks"
author: "PepperPy Team"
plugin_category: "agents"
provider_type: "assistant"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Assistant provider for agents tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("agents", "assistant")
    
    # Usage example here
    ```
