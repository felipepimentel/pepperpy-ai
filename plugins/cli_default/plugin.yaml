name: Default Cli Provider
version: 0.1.0
description: Provider for Default for cli tasks
author: PepperPy Team
plugin_category: cli
provider_type: default
default_config: null
config_schema: null
documentation:
  usage: 'Default provider for cli tasks.


    Example usage:

    ```python

    from pepperpy import create_provider


    provider = create_provider("cli", "default")


    # Usage example here

    ```

    '
category: cli
provider_name: default
entry_point: provider.DefaultProvider
pepperpy_compatibility: '>=0.1.0'
required_config_keys: []
