# Local Hub Provider Plugin Metadata
name: "Local Hub Provider"
version: "0.1.0"
description: "Provider for Local for hub tasks"
author: "PepperPy Team"
plugin_category: "hub"
provider_type: "local"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Local provider for hub tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("hub", "local")
    
    # Usage example here
    ```
