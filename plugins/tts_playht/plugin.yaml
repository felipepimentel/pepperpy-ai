# Playht Tts Provider Plugin Metadata
name: "Playht Tts Provider"
version: "0.1.0"
description: "Provider for Playht for tts tasks"
author: "PepperPy Team"
plugin_category: "tts"
provider_type: "playht"

# Default configuration values
default_config:
  # Add your default config here
  
# Configuration schema
config_schema:
  # Add your config schema here

# Documentation
documentation:
  usage: |
    Playht provider for tts tasks.
    
    Example usage:
    ```python
    from pepperpy import create_provider
    
    provider = create_provider("tts", "playht")
    
    # Usage example here
    ```
