---
title: Logging Best Practices
description: ALWAYS ensure logging follows standard levels and includes necessary error tracking.
globs: "**/*.py"
version: "1.0"
priority: medium
---

<?xml version="1.0" encoding="UTF-8"?>
<rule>
  <metadata>
    <name>logging-best-practices</name>
    <description>ALWAYS ensure logging follows standard levels and includes necessary error tracking.</description>
    <priority>medium</priority>
    <version>1.0</version>
    <tags>
      <tag>logging</tag>
      <tag>observability</tag>
    </tags>
  </metadata>
  <filters>
    <filter>
      <type>code</type>
      <pattern>.*print\(.*\)</pattern>
      <description>Detects use of print statements instead of proper logging.</description>
    </filter>
  </filters>
  <actions>
    <action>
      <type>reject</type>
      <conditions>
        <condition>
          <pattern>.*print\(.*\)</pattern>
          <message>Use of print() detected. Replace with logging.</message>
        </condition>
      </conditions>
    </action>
  </actions>
</rule>
