---
title: Core Security Rules
description: ALWAYS enforce secure coding practices to prevent vulnerabilities and ensure safe execution.
globs: "**/*.py"
version: "1.0"
priority: critical
---

<?xml version="1.0" encoding="UTF-8"?>
<rule>
  <metadata>
    <name>core-security</name>
    <description>ALWAYS enforce secure coding practices to prevent vulnerabilities and ensure safe execution.</description>
    <priority>critical</priority>
    <version>1.0</version>
    <tags>
      <tag>security</tag>
      <tag>vulnerability</tag>
    </tags>
  </metadata>
  <filters>
    <filter>
      <type>code</type>
      <pattern>.*(eval|exec|pickle.load).*</pattern>
      <description>Detects use of unsafe functions.</description>
    </filter>
  </filters>
  <actions>
    <action>
      <type>reject</type>
      <conditions>
        <condition>
          <pattern>.*(eval|exec|pickle.load).*</pattern>
          <message>Use of unsafe functions detected: eval(), exec(), or pickle.load(). Consider safer alternatives.</message>
        </condition>
      </conditions>
    </action>
  </actions>
</rule>
